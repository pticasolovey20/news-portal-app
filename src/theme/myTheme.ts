import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
	palette: {
		primary: {
			main: "#383838",
			contrastText: "#fff",
		},
		secondary: {
			main: "#789012",
		},
	},
});
